import React, { useState } from "react";
import axios from "axios";
const Register = (props) => {
 
  return (
    <div>
      <form className='form' >
      Register
      </form>
    </div>
  );
};

export default Register;